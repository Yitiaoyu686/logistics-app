# 2026-03-16 联调测试数据集说明（海运+空运）

## 1. 一键造数脚本

- 脚本路径：`server/scripts/seed-v2-multi-flow-demo.js`
- 执行命令：

```bash
node /Users/mac/Documents/code/111/server/scripts/seed-v2-multi-flow-demo.js
```

## 2. 本批次数据内容

- 批次：`20260316`
- 客户：新增 6 个（公海 2、私海 4）
- 订单：新增 8 个（海运 4、空运 4）
- 任务：新增 6 个（含“一个任务多集装”场景）
- 无订单待匹配包裹：新增 3 个（SEA 2、AIR 1）
- 路线模板：补充 legacy 路线模板 2 条（SEA/AIR 各 1，用于 full 接口 `routeTemplate`）

## 3. 可直接搜索的订单号

### 海运
- `SEA-ORD-20260316-8059`（待入库）
- `SEA-ORD-20260316-7520`（已入库/待发运）
- `SEA-ORD-20260316-7384`（运输中，多子单，多集装）
- `SEA-ORD-20260316-8976`（已完成，含 DPN/签收）

### 空运
- `AIR-ORD-20260316-2001`（待发运）
- `AIR-ORD-20260316-1856`（运输中）
- `AIR-ORD-20260316-5858`（异常，含待审核费用与流程实例）
- `AIR-ORD-20260316-3990`（已完成，含 DPN/签收）

## 4. 建议测试路径（按环节）

1. 客户中心
- 检查公海/私海客户分布与客户编号唯一性。
- 检查客户档案中的海运/空运业务线默认配置。

2. 订单中心
- 按上面 8 个订单号逐个验证列表筛选、详情字段、状态标签。
- 在详情“物流状态栏”验证 `trackingBySubOrder` 聚合节点是否按子单展示。

3. 任务/集装
- 在运输任务中检查 SEA 运输中单（`SEA-ORD-20260316-7384`）关联任务和两条集装单元。

4. 末端配送
- 在到达国配送查看 DPN 状态（ASSIGNED/SIGNED）及配送任务状态（IN_TRANSIT/SIGNED）。

5. 财务与审核
- 在费用与审核模块检查异常空运单（`AIR-ORD-20260316-5858`）的待审核费用与流程实例。

6. 无订单快递池
- 在无订单列表确认 3 条待匹配包裹，验证推荐挂单与手动挂单操作。

## 5. 数据来源闭环覆盖

本批次数据覆盖：
- CRM：`crm_customer` + 发/收货档案 + 业务线档案
- OMS：`oms_order` / `oms_sub_order` / 初始包裹 / 实际包裹 / 状态日志
- TMS：`tms_job` / `tms_shipping_unit` / `tms_job_order_rel` / `tms_tracking_event`
- POD：`pod_dpn` / `pod_dpn_item` / `pod_delivery_task`
- Finance：`fin_fee` / `fin_payment`
- Workflow：`wf_instance` / `wf_task`
- WMS 无单：`wms_inbound_order` / `wms_inbound_item` / `wms_unmatched_package`

